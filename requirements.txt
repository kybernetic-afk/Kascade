# Pinned to the dependency set that shipped in the working 1.1.0 build.
# cryptography is a transitive dependency of paramiko and is pinned explicitly:
# the unpinned 1.2.0 build pulled cryptography 49.0.0, which stalled SFTP uploads.
paramiko==5.0.0
cryptography==48.0.0
requests==2.34.2
PySide6==6.11.1
